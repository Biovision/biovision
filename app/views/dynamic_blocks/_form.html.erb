<% model_name = entity.class.to_s.underscore %>
<%=
  form_with(
      model: entity,
      html: {
          id: "#{model_name}-form",
          data: { check_url: check_dynamic_blocks_path }
      }
  ) do |f|
%>
  <%= render partial: 'shared/list_of_errors', locals: { entity: entity } %>

  <dl class="fields">
    <div>
      <dt><%= f.label :slug %></dt>
      <dd>
        <%=
          f.text_field(
              :slug,
              class: 'input-text',
              data: { check: :slug },
              maxlength: DynamicBlock::SLUG_LIMIT,
              required: true,
              size: nil
          )
        %>
        <div class="check-result-error" data-field="slug"></div>
        <div class="guideline"><%= t('.guidelines.slug') %></div>
      </dd>
    </div>

    <div>
      <dt><%= f.label :body %></dt>
      <dd>
        <%=
          f.text_area(
              :body,
              class: 'input-text',
              rows: 10
          )
        %>
      </dd>
    </div>

    <%= render partial: 'shared/forms/entity_flags', locals: { f: f } %>
  </dl>

  <%= render 'shared/forms/state_container' %>

  <div class="buttons">
    <%= hidden_field_tag :entity_id, entity.id %>
    <%= f.button t(:save), class: 'button-save' %>
  </div>
<% end %>
